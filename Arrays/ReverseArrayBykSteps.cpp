#include<iostream>
using namespace std;
// 2. Rotate an Array by k Steps
// What It Means:
// You need to shift the elements of the array by k positions.
// There are two types of rotation:
// Right Rotation: Move each element to the right by k positions
// Left Rotation: Move each element to the left by k positions
int main(){
    int arr[] = {1,2,3,4,5,6,7,8,9,0};
    int n = sizeof(arr) / sizeof(arr[0]);

    for(int i = 0 ; i < n; i++){
        
    }

    return 0;
}