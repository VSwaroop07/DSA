#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main()
{
    int arr[] = {4, 2, 5, 3, 6, 1, 8, 7};
    int n = sizeof(arr) / sizeof(arr[0]);
    sort(arr, arr + n);
    int mid = 0;
    if (n % 2 == 0)
    {
        int n1 = 0;
    }
    else
    {

        mid = n / 2;
    }
    for (int i = 0; i < mid; i++){}
}